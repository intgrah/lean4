[package]
name = "lean-expr"
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Rust mirrors of Lean's Expr, Level, Name, LocalContext, MetavarContext with zero-copy conversion to lean_object*."
publish = false

[dependencies]
lean-runtime-sys.workspace = true

[lib]
