# RIIR override symbol list. One C symbol per line; '#' starts a comment.
#
# Each listed symbol gets a -Wl,--wrap=<symbol> linker flag at libleanshared
# link time, redirecting call sites to __wrap_<symbol> (defined in the
# lean-override crate). Use __real_<symbol> to call the original Lean-compiled
# implementation.
#
# Empty until Phase 1 lands the pilot port of lean_is_level_def_eq.
